<div class="block-example {{ blockExample.class }}">
  <div class="container">
    test
    <div class="block-example__grid">{{ blockExample.items }}</div>
    {% if blockExample.image is not empty %}
      <span class="block-example__image">{{ blockExample.image }}</span>
    {% endif %}
  </div>
</div>
